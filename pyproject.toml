[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
extend-exclude = ["python", ".venv-test"]

[tool.ruff.lint]
# Start with correctness checks that pass the existing source tree.
select = ["E9", "F63", "F7", "F82"]
